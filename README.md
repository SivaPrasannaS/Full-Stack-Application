# Full Stack Application

Parent repository for full-stack projects managed as Git submodules.

## Submodules

- Blog Application: CMS blog application containing both frontend and backend code.

## Clone with submodules

```bash
git clone --recurse-submodules <parent-repo-url>
```

## Initialize submodules after clone

```bash
git submodule update --init --recursive
```
